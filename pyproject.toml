[project]
name = "ark-nova-setup-randomizer"
version = "0.1.0"
description = "Simple webtool to randomize the setup of an Ark Nova Game"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.47.1",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
    "isort>=6.0.1",
    "pre-commit>=4.2.0",
]
